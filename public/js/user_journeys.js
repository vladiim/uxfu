(function() {
  var UserJourneys, root;

  root = typeof exports !== "undefined" && exports !== null ? exports : window;

  UserJourneys = (function() {
    function UserJourneys(chart) {
      this.chart = chart;
    }

    return UserJourneys;

  })();

  root.UserJourneys = UserJourneys;

}).call(this);
